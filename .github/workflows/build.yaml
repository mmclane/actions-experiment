name: Building
run-name: ${{ github.actor }} initiated this action 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 This is where building will happen"
