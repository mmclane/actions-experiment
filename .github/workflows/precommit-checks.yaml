name: Check precommit checks
run-name: Check that precommit was run
on: [push]
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 This is where we check that precommit has been run"
